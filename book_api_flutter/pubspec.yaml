name: bookapiflutter
description: A Book Api Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.1
  logger: ^0.9.2
  page_life_cycle: ^1.1.1 # for handle life cycle

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
